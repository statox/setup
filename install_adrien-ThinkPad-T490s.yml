---
- hosts: local
  become: false
  vars_files:
    - "./vars/config.local"

  roles:
    - role: system/base
      become: yes
    - role: system/i3
      become: yes
    - role: system/sudo
      become: yes
    - role: user/base
    - role: user/zsh
    - role: user/nvim
    - role: dashlane/base


# TODO:
# dotfiles
# Remap escape
  # xmodmap -e "keycode 66 = Escape NoSymbol Escape"
  # xmodmap -pke
# Zoom?
# nvm
# docker + docker-compose
# Firefox user chrome
# terraform+vault+aws
